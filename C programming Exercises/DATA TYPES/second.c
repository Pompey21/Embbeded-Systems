#include<stdio.h>


int main(){
	char myString[] = "Today is a sunny day";
	printf("%s\n", myString);
}

// Strings in C are represented as arrays of characters and the last character in that 
// array is called a 'null character', which indicates that this is the end of a string.
